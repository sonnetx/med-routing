__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

.venv/
venv/
.env
.env.local

.pytest_cache/
.ruff_cache/
.mypy_cache/

# SQLite transient sidecars (the .db itself is committed)
*.db-wal
*.db-shm

.cache/
hf_cache/

# Test scratch dirs
audit_test_tmp/

# Pickled artifacts produced by med_routing.train.fit
models/

# Per-run training corpora produced by med_routing.train.collect.
# Reproducible from the committed data/med_routing.db, so we don't ship them.
runs/train_*.jsonl

.DS_Store
