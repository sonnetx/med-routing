apiVersion: 1

datasources:
  - name: SQLite (med-routing audit)
    uid: sqlite-audit
    type: frser-sqlite-datasource
    access: proxy
    editable: false
    jsonData:
      path: /var/lib/grafana-data/med_routing.db
