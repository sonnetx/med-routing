global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: med-routing
    metrics_path: /metrics
    static_configs:
      - targets:
          - app.railway.internal:8000
